numbers = [int(s) for s in input().split()]
min_element = min(numbers)
max_element = max(numbers)
sum_elements = sum(numbers)
print(f"The minimum number is {min_element}")
print(f"The maximum number is {max_element}")
print(f"The sum number is: {sum_elements}")
